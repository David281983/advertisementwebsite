{% extends 'base.html.twig' %}

{% block title %}Create new advertisement{% endblock %}

{% block bread %}
→ Profile Settings
{% endblock %}

{% block body %}
    {{ include('settings_profile/_settings_profile_tabs.html.twig') }}
    {{ form_start(form) }}
    {{ include('settings_profile/_profile_form.html.twig') }}

    <div>
        <button type="submit" class="block w-full shadow-md border-transparent bg-indigo-600 hover:bg-indigo-700 dark:bg-red-600 dark:hover:bg-red-500 text-white rounded-md border p-2 mt-4 mb-2">Save Settings</button>
    </div>
    {{ form_end(form) }}
    <div class="mt-10">
        <h2 class="text-2xl mb-4">My favorites</h2>
        {{ include('advertisement/_advertisement_list.html.twig', {advertisements: favorites}) }}
    </div>
{% endblock %}
